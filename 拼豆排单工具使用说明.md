# 拼豆排单工具使用说明

这是给接单者自己用的订单小本子，不是给顾客下单用的，也不需要登录账号。微信里确认顾客下单后，把订单记到这个网页里，用来减少漏做、漏发和发错人的情况。

## 1. 打开方式

双击打开 `index.html`。

首页上方有三个数字：

- `未发顾客单`：还有多少个顾客的订单没发货。
- `待做订单项`：还有多少个拼豆还没做。
- `待收尾款`：付款状态不是“已付全款”的订单里，还需要收的尾款金额。

## 2. 新增顾客单

点底部的 `新增顾客单`。

必填：

- `顾客微信名`：建议填微信备注名，方便回微信聊天里查。
- `下单日期`：顾客确认下单的日期。
- `最晚发货日期`：答应顾客几号前发货。
- `订单项`：至少填一个。

可选：

- `收货人姓名`
- `收货地区/地址备注`
- `定金`
- `尾款`
- `运费`
- `付款状态`
- `备注`

## 3. 一个顾客订多个拼豆

在新增或编辑订单时，点 `加一项`。

每个订单项可以单独填写：

- `内容描述`：例如“草莓熊挂件”“库洛米头像”“10cm 加名字”。
- `数量`：这个图案要做几个。
- `图片标识`：用来防止相似图片弄混，例如“顾客第 2 张图”“粉色库洛米 A”“聊天记录里那张蓝底图”。
- `待做 / 已做`：这个订单项是否已经做完。

一个顾客有多个订单项时，首页会先显示这个顾客；点开后能看到里面的每个订单项。

## 4. 每天主要看哪里

默认首页只显示 `未发` 的顾客单，并且按 `最晚发货日期` 从近到远排序。

颜色含义：

- 普通颜色：还没到紧急时间。
- 黄色：距离最晚发货日期 2 天以内。
- 红色：已经超过最晚发货日期，但还没标记为已发。

每天先处理红色和黄色订单。

## 5. 做完和发货怎么标记

每个订单项后面有 `待做 / 已做`。

- 做完一个拼豆，就把这一项点成 `已做`。
- 如果一个顾客订了多个拼豆，可以分别标记每一项。

顾客整单后面有 `未发 / 已发`。

- 同一个顾客通常一起发货，所以发货状态按顾客整单算。
- 发货后，把顾客单点成 `已发`。
- 已发订单默认不会出现在未发首页里，可以切到 `已发` 或 `全部` 查看。

## 6. 图片标识怎么用

`图片标识` 不是上传图片，是给微信聊天里的图片做一个对照暗号。

例如：

- `顾客第2张图`
- `粉色库洛米`
- `蓝底小狗`
- `截图A`
- `小红书那张粉色图`

做货或发货前，看这个标识，再回微信聊天里核对原图，避免相似订单发错人。

## 7. 搜索订单

顶部搜索框可以搜索：

- 顾客微信名
- 内容描述
- 图片标识

如果图片相似，建议一定要写清楚 `图片标识`。

## 8. 备份

底部点 `导出`，会下载一个 Excel 备份文件。

建议：

- 每天收工前导出一次。
- 发货高峰期或订单多的时候，多导出几次。
- 把备份文件放到微信文件、网盘或电脑固定文件夹里。

注意：这个工具的数据默认存在当前浏览器里。如果换手机、清浏览器缓存、重装系统，数据可能会丢，所以一定要定期导出备份。

## 9. 恢复备份

底部点 `导入`，选择 Excel 备份文件。

导入方式有两种：

- `合并到当前数据`：保留现在的数据，再把备份里的订单加进来。适合补数据。
- `覆盖当前数据`：清掉现在的数据，只保留备份里的订单。适合换设备恢复。

不确定选哪个时，先选 `合并到当前数据`，更不容易误删。

## 10. 当前限制

- 它是自己统计用的，不是顾客下单系统。
- 数据不自动同步到别的手机或电脑。
- Excel 导入/导出第一次使用时需要联网加载组件。
- 图片不会真正上传保存，只记录图片标识；原图还是要保留在微信聊天里。
