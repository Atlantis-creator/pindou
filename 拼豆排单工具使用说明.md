# 拼豆排单工具使用说明

这是给接单者自己用的手机排单工具，不是给顾客下单用的。顾客在微信里确认下单后，把订单记到这个工具里，用来减少漏做、漏发和发错图。

## 1. 打开方式

打开 `index.html`。

如果以后部署成网页链接，建议用 iPhone 的 Safari 打开，再添加到主屏幕。这样更像 App，入口也更稳定。

首页上方有三个数字：

- `未发顾客单`：还有多少个顾客的订单没发货。
- `待做订单项`：还有多少个拼豆还没做。
- `待收尾款`：付款状态不是“已付全款”的订单里，还需要收的尾款金额。

## 2. 新增顾客单

点底部的 `新增顾客单`。

必填：

- `顾客微信名`：建议填微信备注名，方便回微信聊天里查。
- `下单日期`：顾客确认下单的日期。
- `最晚发货日期`：答应顾客几号前发货。
- `订单项`：至少填一个。

可选：

- `收货人`
- `地址备注`
- `定金`
- `尾款`
- `运费`
- `付款状态`
- `备注`

## 3. 一个顾客订多个拼豆

在新增或编辑订单时，点 `加一项`。

每个订单项可以单独填写：

- `内容描述`：例如“草莓熊挂件”“库洛米头像”“10cm 加名字”。
- `数量`：这个图案要做几个。
- `图片备注`：例如“第 2 张是修改后版本”“按第一张颜色”“微信里还有细节图”。
- `图片`：最多上传 2 张。
- `待做 / 已做`：这个订单项是否已经做完。

一个顾客有多个订单项时，首页会先显示这个顾客；点开后能看到里面的每个订单项。

## 4. 图片怎么用

图片用于核对订单，防止相似图案发错人。

上传规则：

- 每个订单项最多 2 张图片。
- 只支持 JPG、PNG、WebP。
- 上传后会自动压缩成长边 1200px 以内、质量约 0.75。
- 压缩后如果仍然超过 500KB，这张图不会保存。
- 首页只显示第一张缩略图；点图片可以看大图。

注意：工具里保存的是压缩后的核对图，不是高清原图。原图仍然建议保留在微信聊天里。

## 5. 每天主要看哪里

默认首页只显示 `未发` 的顾客单，并且按 `最晚发货日期` 从近到远排序。

颜色含义：

- 普通颜色：还没到紧急时间。
- 黄色：距离最晚发货日期 2 天以内。
- 红色：已经超过最晚发货日期，但还没标记为已发。

每天先处理红色和黄色订单。

## 6. 做完和发货怎么标记

每个订单项后面有 `待做 / 已做`。

- 做完一个拼豆，就把这一项点成 `已做`。
- 如果一个顾客订了多个拼豆，可以分别标记每一项。

顾客整单后面有 `未发 / 已发`。

- 同一个顾客通常一起发货，所以发货状态按顾客整单算。
- 发货后，把顾客单点成 `已发`。
- 如果这单还有订单项是 `待做`，点 `未发` 改成 `已发` 时会先提醒确认。
- 确认已经发货后，这单下面的 `待做` 订单项会自动改成 `已做`。
- 已发订单默认不会出现在未发首页里，可以切到 `已发` 或 `全部` 查看。

## 7. 搜索订单

顶部搜索框可以搜索：

- 顾客微信名
- 内容描述
- 图片备注

如果图片相似，建议一定要写清楚 `图片备注`。

## 8. 完整备份

点底部 `备份`，会导出一个 ZIP 文件。

ZIP 是真正的完整备份，里面包括：

- 订单数据
- 压缩后的订单图片
- 一个可查看的 Excel 表

建议：

- 每天收工前导出一次 ZIP。
- 发货高峰期或订单多的时候，多导出几次。
- 把 ZIP 备份放到微信文件、网盘或电脑固定文件夹里。

注意：这个工具的数据和图片默认存在当前手机浏览器里。如果换手机、清浏览器数据、误删网页应用，数据可能会丢。所以一定要定期导出 ZIP。

## 9. 恢复备份

点底部 `工具`，选择 `导入 ZIP 备份`。

导入时会询问：

- `确定`：覆盖当前数据。
- `取消`：合并到当前数据。

如果不确定，建议选择合并，更不容易误删当前订单。

## 10. Excel 是干什么的

Excel 只用于查看、对账和统计，不是完整恢复备份。

原因是 Excel 不适合稳定保存和恢复图片。真正的恢复请用 ZIP。

## 11. 当前限制

- 它是自己统计用的，不是顾客下单系统。
- 数据和图片不自动同步到别的手机或电脑。
- ZIP 和 Excel 功能第一次使用时需要联网加载组件。
- 图片是压缩核对图，不建议当唯一原图存档。
